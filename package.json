{
  "name": "kelajakmerosi",
  "version": "1.0.0",
  "description": "Kelajak Merosi — full-stack edu platform (React + Node.js/Express + PostgreSQL)",
  "private": true,
  "workspaces": [
    "client",
    "server",
    "shared"
  ],
  "scripts": {
    "start": "npm run dev",
    "dev:client": "cd client && npm run dev",
    "dev:server": "cd server && npm run dev",
    "dev": "concurrently -n CLIENT,SERVER -c cyan,green \"npm run dev:client\" \"npm run dev:server\"",
    "build:client": "cd client && npm run build",
    "build:server": "npm install --prefix server && npm run build --prefix server",
    "start:server": "NODE_PATH=$PWD/server/node_modules:$PWD/node_modules npm start --prefix server",
    "typecheck": "cd client && npm run typecheck",
    "install:all": "cd client && npm install && cd ../server && npm install"
  },
  "dependencies": {
    "zod": "^3.25.76"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}