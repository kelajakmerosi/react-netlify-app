{
  "buildCommand": "npm install --prefix shared && cd client && NPM_CONFIG_PRODUCTION=false npm install && npm run build",
  "outputDirectory": "client/dist",
  "installCommand": "echo skip",
  "framework": null,
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
