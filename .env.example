# ─── EduUz Environment Variables ────────────────────────
# Copy to .env.local and fill in your values

# API base URL (optional — falls back to mock)
VITE_API_URL=https://api.eduuz.uz/v1

# Feature flags
VITE_ENABLE_ANALYTICS=false
